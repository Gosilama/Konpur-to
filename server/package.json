{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "lint": "eslint ../server --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "gosilama",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^6.2.0"
  },
  "devDependencies": {
    "eslint": "^5.14.0"
  }
}
